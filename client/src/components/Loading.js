import React from 'react'
import ReactLoading from 'react-loading'

const Loading = () => (
  <ReactLoading height={100} width={100} type="spinningBubbles" color="#444" delay={0} className="centered"/>
)

export default Loading